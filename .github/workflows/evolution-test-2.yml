name: Forge Evolution Test 2

on:
  push:
    branches:
      - KIRCH-FORGE-ENTITY-EVOLUTION-TEST-2
  workflow_dispatch:

jobs:
  generation-four-evaluator-staging:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Integrated entity validation
        run: python tools/entity_check.py
      - name: Compile Generation 4 evaluator
        run: python -m py_compile tools/evolution_generation4.py

name: Forge Test 3 Level 3 Contract

on:
  push:
    branches:
      - KIRCH-FORGE-ENTITY-EVOLUTION-TEST-3
  workflow_dispatch:

jobs:
  corrected-level3:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python tools/entity_check.py
      - run: python tools/test3_level3_contract.py

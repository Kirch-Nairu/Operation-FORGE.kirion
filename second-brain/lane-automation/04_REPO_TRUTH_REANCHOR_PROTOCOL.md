# Repo Truth Re-Anchor Protocol

Use when the agent drifts, gets confused, or hits unexpected failures.

## Commands

```powershell
git branch --show-current
git rev-parse HEAD
git status --short
git log -1 --oneline
git diff --stat
```

## Rule

Repo truth beats memory and handoff summaries.
